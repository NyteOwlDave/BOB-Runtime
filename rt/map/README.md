
# Map Folder

The `map` file format contains color palettes.
Each color palette contains 256 RGB colors.

This folder also contains `bc` files. This just
a Bob source file containing only color name
definitions as macros.

---

